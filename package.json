{
  "name": "atbbs-workspace",
  "private": true,
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write package.json tsconfig.base.json renovate.json && npm run format --workspaces --if-present",
    "format:check": "prettier --check package.json tsconfig.base.json renovate.json && npm run format:check --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "workspaces": [
    "atbbs",
    "atproto",
    "opengraph",
    "telnet",
    "web"
  ],
  "devDependencies": {
    "oxlint": "1.82.0",
    "prettier": "3.9.6"
  }
}
